<section class="container">
  <h3 class="greenhouse-name">{{greenhouse.name}}</h3>

  <section class="flex-row justify-center">
    {{#each greenhouse.plants as |plant|}}
    {{> plant-details}}
    {{/each}}
  </section>
</section>